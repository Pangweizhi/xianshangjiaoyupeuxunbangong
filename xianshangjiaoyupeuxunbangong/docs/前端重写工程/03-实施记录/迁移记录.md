# 迁移记录

## 2026-05-02

### 工程结构

- 新建独立工作区 `new-frontend`
- 拆分为 `front-web`、`admin-web`、`shared`
- 建立共享请求层、鉴权层、类型层

### 前台迁移

已迁移并重写：

- 首页
- 登录页
- 课程页
- 公告页
- 作业页
- 论坛页
- 备课页
- 会议页
- 个人中心页

同时完成：

- 作业详情与提交
- 论坛详情与回帖
- 资料详情与下载
- 会议详情查看

### 后台迁移

已迁移并重写：

- 仪表盘
- 课程管理
- 公告管理
- 学生管理
- 教师管理
- 字典管理
- 系统配置
- 作业管理
- 论坛管理
- 备课管理
- 会议管理
- 提交记录管理

### 作业提交流程

作业提交已从页面层模拟迁移为后端真实持久化：

- 新增 `zuoye_submit` 表能力
- 前台接入作业提交
- 后台接入提交记录管理
- 接入评分、评语、批改时间、退回重交

## 2026-05-03

### 核心业务一期至三期补齐

根据 `docs/专项功能/核心业务需求具体实现方案.md`，已补齐课程学习主链路：

- 课程审核
- 章节管理
- 资源管理
- 选课
- 学习进度
- 学分记录
- 作业课程化
- 考试与阅卷

### 本次新增后端模块

- `course_chapter`
- `course_resource`
- `course_enroll`
- `study_progress`
- `course_credit_record`
- `exam`
- `exam_question`
- `exam_record`

### 本次扩展既有模块

- `kecheng`
- `zuoye`
- `zuoye_submit`

### 本次新增学生端页面

- 课程详情页
- 我的课程页
- 考试列表页
- 考试详情页
- 我的成绩页

### 本次新增后台页面

- 章节管理
- 资源管理
- 选课管理
- 学习进度
- 考试管理
- 题库管理
- 阅卷管理

### 本次新增学生端能力

- 选课
- 查看章节与课程资源
- 记录学习进度
- 查看学分记录
- 在线考试
- 客观题自动判分
- 主观题待阅卷
- 按规则恢复作答
- 查看考试成绩

### 本次新增后台能力

- 课程审核与资源审核
- 考试规则配置
- 阅卷
- 教师重置未提交考试记录
- 教师作废考试记录

### 本次新增数据库脚本

- `docs/数据库脚本/核心业务一期.sql`
- `docs/数据库脚本/核心业务三期.sql`

### 本次验证结果

已确认：

- 后端 `mvn -q -DskipTests compile` 通过
- `admin-web` 的 `vue-tsc --noEmit` 通过
- `front-web` 的 `vue-tsc --noEmit` 通过

尚未完全确认：

- 浏览器真实联调
- 数据库脚本真实执行后的验收
- `vite build` 受当前环境 `spawn EPERM` 影响

## 2026-05-03（第四期补充）

### 第四期已落地内容

本轮继续补齐核心业务第四期的读取侧与展示侧：

- 新增 `CourseReportController`
- 新增 `CourseReportService`
- 新增 `GET /courseReport/overview`
- 新增 `GET /courseReport/mySummary`

### 本次前端补充

- 管理端 `DashboardView.vue` 接入核心业务第四期总览
- 学生端 `MyScoresView.vue` 改为课程维度成绩汇总视图
- `shared` 增加课程报表与个人成绩汇总类型定义

### 本次可直接查看的数据

- 课程平均学习进度
- 作业平均分与通过数
- 考试平均分与通过数
- 结课人数
- 学分发放数量与总学分
- 学生个人课程维度成绩汇总

### 本次验证结果

已确认：

- 后端 `mvn -q -DskipTests compile` 通过
- `admin-web` 的 `vue-tsc --noEmit` 通过
- `front-web` 的 `vue-tsc --noEmit` 通过

### 第四期后续仍可继续补强

- 报表导出
- 时间区间统计
- 更细的结课规则配置化
- 异常考试日志

## 2026-05-03（智能问答助手一期/二期补记）

### 本次新增专项能力

围绕 `docs/专项功能/智能问答助手具体实现方案.md`，已落地：

- AI 会话持久化
- AI 消息持久化
- 学生端问AI页面
- 后台问AI页面
- 前后台问AI入口
- 猜你想问
- 系统导航类智能问答
- 课程/作业/考试/成绩相关问答
- 百炼兼容接口真实大模型接入

### 本次新增后端模块

- `AiChatController`
- `AiChatSessionService`
- `AiChatMessageService`
- `AiAssistantService`
- `AiModelGatewayService`

### 本次新增前端页面与接口

- `front-web/src/views/AiChatView.vue`
- `admin-web/src/views/AiChatView.vue`
- `front-web/src/api/ai.ts`
- `admin-web/src/api/ai.ts`
- `shared` AI 类型定义

### 本次配置接入

- `application.yml` 新增 `qa.ai` 配置映射
- 环境变量读取：
  - `QA_AI_ENABLED`
  - `QA_AI_API_KEY`
  - `QA_AI_BASE_URL`
  - `QA_AI_MODEL`

### 本次验证结果

- 后端 `mvn -q -DskipTests compile` 通过
- `front-web` 的 `vue-tsc --noEmit` 通过
- `admin-web` 的 `vue-tsc --noEmit` 通过
- 本机环境变量已成功直连百炼兼容接口并返回 `CONNECT_OK`

### 后续可继续补强

- 浏览器端真实问答体验调优
- 提示词模板细化
- 真正的 Function Call
- 章节资源文本抽取
- RAG 增强
- 教师自动出题
